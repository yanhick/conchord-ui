sudo: false
language: haskell
before_install:
- gem install compass
install:
- npm install
- cabal sandbox init
- cabal install --only-dependencies --enable-tests
script:
- npm test
- cabal configure --enable-tests && cabal test
cache:
  directories:
  - node_modules
  - .cabal-sandbox
